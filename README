Sliding Window Protocol which sends a file (here fileX) from a sender to a reciever and creates a new identical file,
detecting CORRUPT FRAMES, REORDERED FRAMES and MISSING FRAMES.
Use: ./run_experiment.sh to compile.
